server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: event-service
          uri: lb://event-service
          predicates:
            - Path=/api/events/**,/api/host/events/**

        - id: venue-service
          uri: lb://venue-service
          predicates:
            - Path=/api/venues,/api/venues/**,/api/admin/venues/**

        - id: auth-service
          uri: lb://auth-service
          predicates:
            - Path=/api/auth/**
        
        - id: user-service
          uri: lb://auth-service
          predicates:
            - Path=/api/user/**,/api/admin/users/**
        
        - id: transaction-service
          uri: lb://transaction-service
          predicates:
            - Path=/api/user/bookings/**,/api/user/notifications/**
        
        - id: host-mgnt-service
          uri: lb://host-mgnt-service
          predicates:
            - Path=/api/user/hosts/**,/api/admin/hosts/**,/api/host/requests/**
eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: ERROR
    org.springframework: ERROR
    org.hibernate: ERROR

app:
  jwt:
    secret: "our-very-secure-and-very-long-secret-key-at-least-32-chars-long"
    expiration-ms: 86400000 # 24 hours